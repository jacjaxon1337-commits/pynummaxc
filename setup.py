from setuptools import setup, find_packages

setup(
    name="py_mathx_pro",
    version="0.1.0",
    description="A lightweight Python engine for basic arithmetic operations.",
    packages=find_packages(),
    python_requires=">=3.7",
)
